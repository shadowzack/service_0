{
  "name": "service_0",
  "version": "1.0.0",
  "description": "no description deal with it (technically this is a description)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mahmod",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3"
  }
}
